{
    "name": "proveway",
    "version": "1.0.0",
    "private": true,
    "scripts": {
      "build": "mkdir -p dist && cp -r src/. dist/"
    },
    "devDependencies": {
      "cross-env": "^7.0.3"
    }
  }